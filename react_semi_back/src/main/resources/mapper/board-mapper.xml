<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.co.iei.board.model.dao.BoardDao">

	<select id="selectBoardList" resultType="board">
	select 
		board_no,
		board_category,
		board_title,
		board_thumb,
		board_writer,
		to_char(board_date,'yyyy-mm-dd') as board_date,
        member_thumb
	from board_tbl 
    join member_tbl on (board_writer = member_id)
	where board_status = #{status}

	<!-- 카테고리 -->
    <if test="category == 1">
        and board_category = 1
    </if>
    <if test="category == 2">
        and board_category = 2
    </if>

	<if test="searchType == 1">
		and board_title like '%'||#{searchKeyword}||'%'
	</if>
	<if test="searchType == 2">
		and board_writer = #{searchKeyword}
	</if>

	<if test="order == 1">
		order by board_no desc
	</if>
	<if test="order == 2">
		order by board_no asc
	</if>

	offset #{page} * #{size} rows fetch next #{size} rows only
</select>

<select id="selectBoardCount" resultType="Integer">
	select count(*) from board_tbl
	where board_status = #{status}

    <if test="category == 1">
        and board_category = 1
    </if>
    <if test="category == 2">
        and board_category = 2
    </if>

	<if test="searchType == 1">
		and board_title like '%'||#{searchKeyword}||'%'
	</if>
	<if test="searchType == 2">
		and board_writer = #{searchKeyword}
	</if>
</select>

<select id="getNewBoardNo" resultType="int">
    select board_seq.nextval from dual
</select>

<insert id="inserBoard">
    insert into board_tbl (
        board_no,
        board_category,
        board_title,
        board_content,
        board_thumb,
        board_writer,
        board_date,
        board_status,
        attraction_no,
        location_no,
        place_name,
        address_name
    ) values (
        #{boardNo},
        #{boardCategory},
        #{boardTitle},
        #{boardContent},
        #{boardThumb, jdbcType=VARCHAR},     #{boardWriter},
        sysdate,
        1,
        #{attractionNo, jdbcType=NUMERIC},   #{locationNo, jdbcType=VARCHAR},     #{placeName, jdbcType=VARCHAR},      #{addressName, jdbcType=VARCHAR}     )
</insert>

</mapper>
